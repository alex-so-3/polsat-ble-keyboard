# ESPHome
.esphome/
secrets.yaml
.venv/
__pycache__/

# ESP-IDF build artifacts (the working tree is shared with the `main` branch,
# which builds the ESP-IDF firmware; never commit these)
build/
.cache/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Editor / tooling
.vscode/
.idea/
*.swp
compile_commands.json

# macOS
.DS_Store
